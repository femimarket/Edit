<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ImageEdit is a pure library screen: ProjectService I/O is local
         (filesDir); the screen makes no network calls. The host app (demo)
         supplies the launcher activity. -->

</manifest>
